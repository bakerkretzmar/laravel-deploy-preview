{
  "name": "@tighten/deploy-preview",
  "version": "0.1.0",
  "license": "MIT",
  "description": "",
  "keywords": [],
  "author": "Jacob Baker-Kretzmar",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bakerkretzmar/deploy-preview.git"
  },
  "bugs": {
    "url": "https://github.com/bakerkretzmar/deploy-preview/issues"
  },
  "homepage": "https://github.com/bakerkretzmar/deploy-preview#readme",
  "scripts": {
    "run": "ncc run index.ts",
    "test": "vitest",
    "build": "tsc",
    "compile": "ncc build --minify --source-map --license licenses.txt",
    "package": "npm run build && npm run compile",
    "build-app": "npm run build && ncc build --out ../deploy-preview-app/dist && rsync -a action.yml ../deploy-preview-app"
  },
  "type": "module",
  "main": "lib/index.js",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@actions/http-client": "^2.0.1"
  },
  "devDependencies": {
    "@octokit/webhooks-definitions": "^3.67.3",
    "@types/node": "^18.11.0",
    "@vercel/ncc": "^0.34.0",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4"
  },
  "prettier": {
    "printWidth": 120,
    "singleQuote": true,
    "tabWidth": 2,
    "trailingComma": "es5"
  }
}
